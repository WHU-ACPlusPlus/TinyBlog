# ── 库副本 / 本地暂存 ──
lib/

# ── SQLite 数据库（运行时数据，不提交） ──
*.db
*.db-shm
*.db-wal
*.db-journal

# ── Python ──
__pycache__/
*.pyc
.venv/
.uv/

# ── Qt/C++ 前端构建产物 ──
src/frontend/build/
src/frontend/.qmlls.ini

# ── 编辑器/IDE ──
.idea/
.vscode/
.qtcreator/
*.swp
*.swo
*~

# ── macOS ──
.DS_Store

# ── 环境变量 / 密钥 ──
.env
